import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#0000cd',
    marginBottom: 8,
    padding: 12,
    borderRadius: 4,
  },
  title: {
    fontWeight: 'bold',
  },
  text: {
    color: 'white',
    fontSize: 16,
  },
  companyIdBox: {
    flexDirection: 'row',
    gap: 8 ,
    alignItems: 'center',
  },
});
