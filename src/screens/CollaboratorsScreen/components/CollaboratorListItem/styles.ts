import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  containerItem: {
    backgroundColor: '#FDFDFD',
    padding: 8,
    marginBottom: 8,
  },
  statusText: {
    fontSize: 12,
  },
  fullNameText: {
    fontWeight: 600,
  },
});
